# S2.1 — Judgment Corpus & Keyword Search

- Status: closed
- Theme: 100K+ Pakistani reported/unreported judgments with FTS keyword search + citation lookup
- Phase: Phase 2 — Legal Intelligence & Retrieval
- Session cap: 8 items
- Reviewed date / URL / evidence path: 2026-07-02 / live (Railway) / retrospective
- Build gate this batch? yes
- Active root: `ai-legal-system/`
- Source links: `docs/blueprint/vision.md`, `docs/blueprint/prd.md`, `docs/blueprint/architecture.md`, `docs/blueprint/phase-plan.md`
- Handoff status target: READY

## Why this is one buildable unit
The judgment corpus, its full-text index, the search/lookup APIs, and the case-law browsing page form one retrieval unit: a lawyer types a query or a citation and gets ranked, real judgments back — verifiable end-to-end in one pass (query → hit list → open full judgment).

## Traceability (Blueprint intent — the build verifies against this)
| This segment satisfies | IDs |
|------------------------|-----|
| Phase | Phase 2 |
| Architecture components | C5, C18 |
| PRD requirements | R4.1 |
| Vision capabilities | CAP-4 |

## Acceptance (definition of done for the segment)
- [ ] `/case-law` runs a keyword/citation query and returns ranked judgments with court, year, province, and reported status.
- [ ] `/api/judgments/local` performs FTS keyword search over `data/judgments.db`; `/api/judgments/local/[id]` returns the full parsed judgment text.
- [ ] Citation lookup (SCMR/PLD/PCrLJ/MLD/CLC/YLR/PLJ/NLR/SBLR + year/page) resolves to the matching judgment when it exists in the corpus.
- [ ] Corpus is read-only at runtime; Prisma `LegalJudgment`/`LegalJudgmentTag` carry court, year, province, reportedStatus, reliability, and area/type classification.
- [ ] Test expectation: integration tests assert keyword hits, citation resolution, `[id]` full-text return, and empty-result handling for unknown citations.
- [ ] NFR checks: keyword search p95 < 1s; corpus read-only; results carry provenance (court/year/reported).
- [ ] Verification: `GET /api/judgments/local?q=<term>` → ranked hits; `GET /api/judgments/local/<id>` → full judgment body.

## Batch summary
- Ship-ready: `/case-law` page, local search + `[id]` APIs, judgment-db + runtime + retrieval + parse libs, `data/judgments.db` FTS, `LegalJudgment`/`LegalJudgmentTag`
- Must-fix: none
- Redesign / rework candidates: none
- Open questions for the user: none

## Item checklist
| Item | Locator | Status |
|------|---------|--------|
| Case-law browse/search page | `src/app/(dashboard)/case-law/page.tsx` (`/case-law`) | closed |
| Local search API | `src/app/api/judgments/local/route.ts` | closed |
| Full-judgment API | `src/app/api/judgments/local/[id]/route.ts` | closed |
| Judgment DB libs | `src/lib/judgment-db.ts`, `src/lib/judgment-db-runtime.ts` | closed |
| Retrieval + parse libs | `src/lib/judgment-retrieval.ts`, `src/lib/judgment-parse.ts` | closed |
| Read-only FTS corpus | `data/judgments.db` | closed |
| Prisma judgment models | `LegalJudgment`, `LegalJudgmentTag` | closed |

## Item notes (one full section per item)

### JUD-01 — Case-law page & search/lookup APIs
- Evidence (screenshot / output path): live route `/case-law`; `src/app/api/judgments/local/route.ts`, `src/app/api/judgments/local/[id]/route.ts`

| Findings | Severity | Category | Evidence | Recommendation | Status |
|----------|----------|----------|----------|----------------|--------|
| No major findings (shipped/live) |  |  |  |  | closed |

**Decision:** ship-ready

### JUD-02 — Corpus, FTS index & classification (libs + db + Prisma models)
- Evidence (screenshot / output path): `src/lib/judgment-db.ts`, `src/lib/judgment-db-runtime.ts`, `src/lib/judgment-retrieval.ts`, `src/lib/judgment-parse.ts`, read-only `data/judgments.db` (FTS), Prisma `LegalJudgment`/`LegalJudgmentTag` (court, year, province, reportedStatus, reliability, area/type tags)

| Findings | Severity | Category | Evidence | Recommendation | Status |
|----------|----------|----------|----------|----------------|--------|
| No major findings (shipped/live) |  |  |  |  | closed |

**Decision:** ship-ready

## Completion gate (before status `closed`)
- [x] Every worked item has a full notes section in THIS file
- [x] Acceptance criteria above all met and verified
- [x] Traceability still holds
- [x] Verify command run and result recorded
- [x] Build ledger updated (`docs/blueprint/blueprint-progress.md`)
- [x] Handoff updated (`docs/blueprint/next-steps-handoff.md`)
